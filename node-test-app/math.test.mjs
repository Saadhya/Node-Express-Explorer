import { isOdd, timesTwo } from "./math.js";
// import assert from "node:assert";
import assert from "node:assert/strict";

// throw new Error('this is an error');
// assert.ok(timesTwo(3));
// assert.equal(timesTwo(3), 5);
// assert.notEqual(timesTwo(3), 5);
// assert.notDeepEqual(timesTwo(3), 6);
// assert.strictEqual(timesTwo(3), 6);

// for asset/strict- by default everything is strict
// assert.equal(isOdd(2), true);

// check assertions with implementation
// const rand= Math.floor(Math.random()*1_000_000)
// const isRandOdd= rand*2===1;
// assert.equal(isOdd(rand), isRandOdd);

// writing good assertion 
// assert.equal(isOdd(-5), true);
